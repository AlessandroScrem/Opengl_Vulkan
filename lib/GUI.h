#pragma once
#include <model.hpp>
#include <string>
#include <vector>

namespace GUI
{

    bool ObjectNode(Transformations &transf, std::vector<std::string> &items, size_t &item_current_idx);
    
    
} // namespace name
