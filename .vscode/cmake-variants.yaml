buildType:
  default: debug
  choices:
    debug:
      short: Debug
      long: Emit debug information
      buildType: Debug
    release:
      short: Release
      long: Optimize generated code
      buildType: Release

engine:
  default: Vulkan
  choices:
    Vulkan:
      short: Vulkan
      settings:
        ENGINE: VULKAN

    Opengl:
      short: Opengl
      settings:
        ENGINE: OPENGL

    TestApp:
      short: Test app
      settings:
        TEST_APP: true

